apiVersion: v1
kind: Service
metadata:
  name: {{ include "name-resolution-api.fullname" . }}
  labels:
    {{- include "name-resolution-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containers.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "name-resolution-api.selectorLabels" . | nindent 4 }}
